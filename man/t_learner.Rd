% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metalearners.R
\name{t_learner}
\alias{t_learner}
\alias{predict.t_learner}
\title{T-Learner for CATE estimation}
\usage{
t_learner(
  data,
  outcome = "y",
  treatment = "t",
  learner,
  learner1 = NULL,
  covariates = NULL
)

\method{predict}{t_learner}(object, newdata, ...)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{learner}{An mlr3 regression learner used for the control-group
model (and, if \code{learner1} is \code{NULL}, also for the treated-group model).}

\item{learner1}{Optional separate mlr3 regression learner for the treated
group. Defaults to a clone of \code{learner}.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{object}{A fitted \code{t_learner} model.}

\item{newdata}{A \code{data.frame} containing at least the covariate columns.}

\item{...}{Ignored.}
}
\value{
A fitted CATE model of class \code{c("t_learner", "cate_learner")};
see \code{\link[=s_learner]{s_learner()}}.
}
\description{
The T-Learner ("two learners") fits separate outcome models for the
control and treated groups,
\eqn{\mu_0(x) = E[Y | X = x, T = 0]} and
\eqn{\mu_1(x) = E[Y | X = x, T = 1]}, and estimates the CATE as their
difference:
\deqn{\hat\tau(x) = \hat\mu_1(x) - \hat\mu_0(x)}
}
\section{Methods (by generic)}{
\itemize{
\item \code{predict(t_learner)}: Predict CATEs for new data.

}}
\examples{
\donttest{
library(mlr3)
data(synth_train)
data(synth_test)
m <- t_learner(synth_train, outcome = "y", treatment = "t",
               learner = lrn("regr.rpart"))
tau_hat <- predict(m, synth_test)
pehe(synth_test$tau, tau_hat)
}

}
\references{
Künzel, S. R., Sekhon, J. S., Bickel, P. J., & Yu, B. (2019).
Metalearners for estimating heterogeneous treatment effects using machine
learning. \emph{Proceedings of the National Academy of Sciences}, 116(10),
4156-4165.
}
\seealso{
\code{\link[=s_learner]{s_learner()}}, \code{\link[=x_learner]{x_learner()}}
}
