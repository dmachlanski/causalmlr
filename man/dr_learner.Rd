% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metalearners.R
\name{dr_learner}
\alias{dr_learner}
\alias{predict.dr_learner}
\title{DR-Learner for CATE estimation}
\usage{
dr_learner(
  data,
  outcome = "y",
  treatment = "t",
  outcome_learner,
  ps_learner,
  tau_learner = NULL,
  covariates = NULL,
  folds = 5,
  ps_trim = NULL
)

\method{predict}{dr_learner}(object, newdata, ...)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{outcome_learner}{An mlr3 regression learner for the stage-1 outcome
model, e.g. \code{mlr3::lrn("regr.ranger")}. The treatment indicator is
included as a feature and potential outcomes are predicted by setting it
to 0 and 1.}

\item{ps_learner}{An mlr3 classification learner used as the propensity
score model, e.g. \code{mlr3::lrn("classif.log_reg")}. Its predict type is
set to \code{"prob"} automatically.}

\item{tau_learner}{Optional mlr3 regression learner for the second-stage
pseudo-outcome regression. Defaults to a clone of \code{outcome_learner}.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{folds}{Number of cross-fitting folds for the nuisance models.
Default \code{5}. Cross-fitting is a core ingredient of the DR-Learner;
\code{folds = 1} (in-sample nuisance estimates) is allowed but not recommended
with flexible learners.}

\item{ps_trim}{Optional trimming for the estimated propensity scores.
Either a single value \code{a} (clips to \verb{[a, 1 - a]}) or a length-2 range.
\code{NULL} (default) applies no trimming.}

\item{object}{A fitted \code{dr_learner} model.}

\item{newdata}{A \code{data.frame} containing at least the covariate columns.}

\item{...}{Ignored.}
}
\value{
A fitted CATE model of class \code{c("dr_learner", "cate_learner")}.
Use \code{\link[=predict]{predict()}} to obtain CATE estimates for new data and \code{\link[=ate]{ate()}} for
their average.
}
\description{
The DR-Learner ("doubly robust learner"; Kennedy, 2023) is a two-stage
meta-learner that turns the augmented inverse propensity weighting (AIPW)
score used by \code{\link[=ate_dr]{ate_dr()}} into a model that predicts individual CATEs.
\enumerate{
\item Cross-fit the nuisance functions: the propensity score
\eqn{\hat e(x) = P(T = 1 | X = x)} and the potential-outcome models
\eqn{\hat\mu_0(x)}, \eqn{\hat\mu_1(x)} from a single outcome model that
includes the treatment as a feature.
\item Form the doubly robust pseudo-outcome
\deqn{\psi_i = \hat\mu_1(X_i) - \hat\mu_0(X_i) +
     \frac{T_i (Y_i - \hat\mu_1(X_i))}{\hat e(X_i)} -
     \frac{(1 - T_i)(Y_i - \hat\mu_0(X_i))}{1 - \hat e(X_i)}}
and regress it on the covariates to obtain the final CATE model
\eqn{\hat\tau(x) = E[\psi | X = x]}.
}
}
\details{
Because the pseudo-outcome has conditional mean equal to the true CATE
whenever either nuisance is correctly specified, the second-stage
regression targets the CATE directly. Cross-fitting the nuisances (the
default \code{folds = 5}) makes the pseudo-outcome orthogonal to nuisance
estimation error, so flexible learners can be used without overfitting
bias. Unlike \code{\link[=ate_dr]{ate_dr()}}, which averages \eqn{\psi_i} to a scalar ATE, the
DR-Learner keeps the second-stage model and can therefore predict CATEs on
new test data.
}
\section{Methods (by generic)}{
\itemize{
\item \code{predict(dr_learner)}: Predict CATEs for new data.

}}
\examples{
\donttest{
library(mlr3)
data(synth_train)
data(synth_test)
set.seed(1)
m <- dr_learner(synth_train, outcome = "y", treatment = "t",
                outcome_learner = lrn("regr.rpart"),
                ps_learner = lrn("classif.rpart"),
                ps_trim = 0.01)
tau_hat <- predict(m, synth_test)
pehe(synth_test$tau, tau_hat)
}

}
\references{
Kennedy, E. H. (2023). Towards optimal doubly robust estimation of
heterogeneous causal effects. \emph{Electronic Journal of Statistics}, 17(2),
3008-3049.
}
\seealso{
\code{\link[=s_learner]{s_learner()}}, \code{\link[=t_learner]{t_learner()}}, \code{\link[=x_learner]{x_learner()}}, \code{\link[=r_learner]{r_learner()}},
\code{\link[=ate_dr]{ate_dr()}}
}
