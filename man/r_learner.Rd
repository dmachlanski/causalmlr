% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metalearners.R
\name{r_learner}
\alias{r_learner}
\alias{predict.r_learner}
\title{R-Learner for CATE estimation}
\usage{
r_learner(
  data,
  outcome = "y",
  treatment = "t",
  outcome_learner,
  ps_learner,
  tau_learner = NULL,
  covariates = NULL,
  folds = 5,
  ps_trim = NULL
)

\method{predict}{r_learner}(object, newdata, ...)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{outcome_learner}{An mlr3 regression learner for the conditional
outcome mean \eqn{m(x) = E[Y | X]} (the treatment is excluded from the
features), e.g. \code{mlr3::lrn("regr.ranger")}.}

\item{ps_learner}{An mlr3 classification learner used as the propensity
score model, e.g. \code{mlr3::lrn("classif.log_reg")}. Its predict type is
set to \code{"prob"} automatically.}

\item{tau_learner}{Optional mlr3 regression learner for the second-stage
weighted pseudo-outcome regression. Defaults to a clone of
\code{outcome_learner}.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{folds}{Number of cross-fitting folds for the nuisance models.
Default \code{5}. Cross-fitting is a core ingredient of the DR-Learner;
\code{folds = 1} (in-sample nuisance estimates) is allowed but not recommended
with flexible learners.}

\item{ps_trim}{Optional trimming for the estimated propensity scores.
Either a single value \code{a} (clips to \verb{[a, 1 - a]}) or a length-2 range.
\code{NULL} (default) applies no trimming.}

\item{object}{A fitted \code{r_learner} model.}

\item{newdata}{A \code{data.frame} containing at least the covariate columns.}

\item{...}{Ignored.}
}
\value{
A fitted CATE model of class \code{c("r_learner", "cate_learner")}.
Use \code{\link[=predict]{predict()}} to obtain CATE estimates for new data and \code{\link[=ate]{ate()}} for
their average.
}
\description{
The R-Learner (Nie & Wager, 2021) is the heterogeneous-effect
generalisation of the partially linear DML estimator (\code{\link[=ate_dml]{ate_dml()}}). It
builds on the Robinson decomposition of the model
\eqn{Y = \tau(X) T + g(X) + \varepsilon}:
\enumerate{
\item Cross-fit the two nuisance functions, the conditional outcome mean
\eqn{\hat m(x) = E[Y | X = x]} (the treatment is excluded from the
features) and the propensity score \eqn{\hat e(x) = P(T = 1 | X = x)},
and form the residuals \eqn{\tilde Y = Y - \hat m(X)} and
\eqn{\tilde T = T - \hat e(X)}.
\item Estimate the CATE by minimising the R-Loss
\deqn{\hat\tau = \arg\min_\tau \frac{1}{n} \sum_i \left[ \tilde Y_i -
     \tilde T_i \, \tau(X_i) \right]^2}
which is fitted as a weighted regression of the pseudo-outcome
\eqn{\tilde Y_i / \tilde T_i} on the covariates with weights
\eqn{\tilde T_i^2}.
}
}
\details{
The objective minimised here is exactly the \code{\link[=r_loss]{r_loss()}} used elsewhere for
model selection, so the R-Learner is the estimator that directly targets
that score. When the treatment effect is constant it reduces to
\code{\link[=ate_dml]{ate_dml()}}; unlike that scalar estimator, the R-Learner keeps the
second-stage model and can predict CATEs on new test data.

The weighted formulation requires a \code{tau_learner} that supports
observation weights (\code{"weights" \%in\% learner$properties}). If it does not,
the R-Learner falls back to an unweighted regression of the pseudo-outcome
and issues a warning. As with \code{\link[=ate_dml]{ate_dml()}}, small \eqn{\tilde T_i} inflate
the pseudo-outcome, so \code{ps_trim} is recommended with flexible propensity
learners.
}
\section{Methods (by generic)}{
\itemize{
\item \code{predict(r_learner)}: Predict CATEs for new data.

}}
\examples{
\donttest{
library(mlr3)
data(synth_train)
data(synth_test)
set.seed(1)
m <- r_learner(synth_train, outcome = "y", treatment = "t",
               outcome_learner = lrn("regr.rpart"),
               ps_learner = lrn("classif.rpart"),
               ps_trim = 0.01)
tau_hat <- predict(m, synth_test)
pehe(synth_test$tau, tau_hat)
}

}
\references{
Nie, X., & Wager, S. (2021). Quasi-oracle estimation of heterogeneous
treatment effects. \emph{Biometrika}, 108(2), 299-319.
}
\seealso{
\code{\link[=s_learner]{s_learner()}}, \code{\link[=t_learner]{t_learner()}}, \code{\link[=x_learner]{x_learner()}}, \code{\link[=dr_learner]{dr_learner()}},
\code{\link[=ate_dml]{ate_dml()}}, \code{\link[=r_loss]{r_loss()}}
}
