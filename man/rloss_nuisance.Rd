% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{rloss_nuisance}
\alias{rloss_nuisance}
\title{Nuisance models for the R-Loss}
\usage{
rloss_nuisance(
  data,
  outcome = "y",
  treatment = "t",
  outcome_learner,
  ps_learner,
  covariates = NULL,
  folds = 5,
  ps_trim = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.
Typically a held-out validation set that was not used to train the
CATE models being evaluated.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{outcome_learner}{An mlr3 regression learner for \code{m(x)} (the
treatment is excluded from the features).}

\item{ps_learner}{An mlr3 classification learner for \code{e(x)}.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{folds}{Number of cross-fitting folds. Default \code{5}; \code{1} disables
cross-fitting.}

\item{ps_trim}{Optional trimming for the estimated propensity scores;
see \code{\link[=ate_ipw]{ate_ipw()}}.}
}
\value{
An object of class \code{"rloss_nuisance"} with elements \code{y_res},
\code{t_res}, \code{m_hat} and \code{e_hat}, to be passed to \code{\link[=r_loss]{r_loss()}}.
}
\description{
Estimates the two nuisance functions required by the R-Loss (\code{\link[=r_loss]{r_loss()}}):
the conditional outcome mean \eqn{m(x) = E[Y | X = x]} and the propensity
score \eqn{e(x) = P(T = 1 | X = x)}, and returns the corresponding
residuals \eqn{Y - \hat m(X)} and \eqn{T - \hat e(X)}. By default the
nuisance predictions are cross-fitted (out-of-fold), which avoids
overfitting bias; set \code{folds = 1} for simple in-sample estimates.
}
\details{
Fitting the nuisance models once and reusing the resulting object to
score many candidate CATE models (e.g. across a hyperparameter grid) is
both faster and methodologically cleaner, since all candidates are
compared against the same residuals.
}
\examples{
\donttest{
library(mlr3)
data(synth_train)
set.seed(1)
nuis <- rloss_nuisance(synth_train, outcome = "y", treatment = "t",
                       outcome_learner = lrn("regr.rpart"),
                       ps_learner = lrn("classif.rpart"))
m <- s_learner(synth_train, outcome = "y", treatment = "t",
               learner = lrn("regr.rpart"))
r_loss(nuis, predict(m, synth_train))
}

}
\seealso{
\code{\link[=r_loss]{r_loss()}}
}
