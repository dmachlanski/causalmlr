% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metalearners.R
\name{cate_ci}
\alias{cate_ci}
\title{Bootstrap confidence intervals for predicted CATEs}
\usage{
cate_ci(
  object,
  newdata,
  train_data,
  n_boot = 200,
  level = 0.95,
  type = c("percentile", "normal"),
  ...
)
}
\arguments{
\item{object}{A fitted meta-learner of class \code{"cate_learner"}.}

\item{newdata}{A \code{data.frame} of points at which to predict CATEs and their
intervals; must contain the covariate columns.}

\item{train_data}{The \code{data.frame} the model was fitted on (with the
outcome, treatment and covariate columns), used to draw the bootstrap
resamples on which the pipeline is refitted.}

\item{n_boot}{Number of bootstrap replicates. Default \code{200}.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{type}{Interval type. \code{"percentile"} (default) uses the empirical
quantiles of the bootstrap CATEs; \code{"normal"} centres the interval on the
point estimate and uses a normal-quantile multiple of the bootstrap
standard error.}

\item{...}{Ignored.}
}
\value{
A \code{data.frame} with one row per row of \code{newdata} and columns
\code{estimate} (the point CATE from \code{object}), \code{se} (the bootstrap standard
error) and \code{lower}/\code{upper} (the confidence limits).
}
\description{
Attaches pointwise confidence intervals to the CATE predictions of any
fitted meta-learner (\code{\link[=s_learner]{s_learner()}}, \code{\link[=t_learner]{t_learner()}}, \code{\link[=x_learner]{x_learner()}},
\code{\link[=dr_learner]{dr_learner()}}, \code{\link[=r_learner]{r_learner()}}) using the nonparametric bootstrap. For each
of \code{n_boot} replicates the training data is resampled with replacement, the
entire learner pipeline (including any nuisance models and cross-fitting) is
refitted, and CATEs are predicted for \code{newdata}. A pointwise interval is
then formed from the bootstrap distribution at each row of \code{newdata}.
}
\details{
Because the meta-learners accept arbitrary (and typically non-smooth)
machine-learning base learners, no closed-form standard error is available
for \eqn{\hat\tau(x)}; the bootstrap is the one mechanism that applies
uniformly across all five learners. Two caveats are worth keeping in mind.
First, bootstrap coverage for CATEs estimated with flexible learners is only
approximate and can be anti-conservative, so the intervals are best read as
a measure of estimation stability rather than exact frequentist coverage.
Second, refitting the full pipeline \code{n_boot} times is computationally
expensive. Set the seed with \code{\link[=set.seed]{set.seed()}} beforehand for reproducibility.
}
\examples{
\donttest{
library(mlr3)
data(synth_train)
data(synth_test)
set.seed(1)
m <- t_learner(synth_train, outcome = "y", treatment = "t",
               learner = lrn("regr.rpart"))
ci <- cate_ci(m, synth_test, train_data = synth_train, n_boot = 50)
head(ci)
}

}
\seealso{
\code{\link[=s_learner]{s_learner()}}, \code{\link[=t_learner]{t_learner()}}, \code{\link[=x_learner]{x_learner()}}, \code{\link[=dr_learner]{dr_learner()}},
\code{\link[=r_learner]{r_learner()}}
}
