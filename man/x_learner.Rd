% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metalearners.R
\name{x_learner}
\alias{x_learner}
\alias{predict.x_learner}
\title{X-Learner for CATE estimation}
\usage{
x_learner(
  data,
  outcome = "y",
  treatment = "t",
  learner,
  ps_learner,
  tau_learner = NULL,
  covariates = NULL
)

\method{predict}{x_learner}(object, newdata, ...)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{learner}{An mlr3 regression learner for the stage-1 outcome models.}

\item{ps_learner}{An mlr3 classification learner for the propensity score
model, e.g. \code{mlr3::lrn("classif.log_reg")}.}

\item{tau_learner}{Optional mlr3 regression learner for the stage-2
imputed-effect models. Defaults to a clone of \code{learner}.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{object}{A fitted \code{x_learner} model.}

\item{newdata}{A \code{data.frame} containing at least the covariate columns.}

\item{...}{Ignored.}
}
\value{
A fitted CATE model of class \code{c("x_learner", "cate_learner")};
see \code{\link[=s_learner]{s_learner()}}.
}
\description{
The X-Learner (Künzel et al., 2019) proceeds in three stages:
\enumerate{
\item Fit group-specific outcome models \eqn{\hat\mu_0(x)} and
\eqn{\hat\mu_1(x)} as in the T-Learner.
\item Impute individual treatment effects,
\eqn{D_0 = \hat\mu_1(X_0) - Y_0} for controls and
\eqn{D_1 = Y_1 - \hat\mu_0(X_1)} for the treated, and regress them on
the covariates to obtain \eqn{\hat\tau_0(x)} and \eqn{\hat\tau_1(x)}.
\item Combine the two estimates with propensity score weights:
\deqn{\hat\tau(x) = \hat e(x) \hat\tau_0(x) +
     (1 - \hat e(x)) \hat\tau_1(x)}
}
}
\details{
The X-Learner is particularly effective when the treated and control
groups are of very different sizes.
}
\section{Methods (by generic)}{
\itemize{
\item \code{predict(x_learner)}: Predict CATEs for new data.

}}
\examples{
\donttest{
library(mlr3)
data(synth_train)
data(synth_test)
m <- x_learner(synth_train, outcome = "y", treatment = "t",
               learner = lrn("regr.rpart"),
               ps_learner = lrn("classif.rpart"))
tau_hat <- predict(m, synth_test)
pehe(synth_test$tau, tau_hat)
}

}
\references{
Künzel, S. R., Sekhon, J. S., Bickel, P. J., & Yu, B. (2019).
Metalearners for estimating heterogeneous treatment effects using machine
learning. \emph{Proceedings of the National Academy of Sciences}, 116(10),
4156-4165.
}
\seealso{
\code{\link[=s_learner]{s_learner()}}, \code{\link[=t_learner]{t_learner()}}
}
