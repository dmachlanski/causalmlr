% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ate.R
\name{ate_dml}
\alias{ate_dml}
\title{Double machine learning (DML) ATE estimator}
\usage{
ate_dml(
  data,
  outcome = "y",
  treatment = "t",
  outcome_learner,
  ps_learner,
  covariates = NULL,
  folds = 5,
  ps_trim = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{outcome_learner}{An mlr3 regression learner for the conditional
outcome mean \code{m(x) = E[Y | X]} (treatment excluded from the features).}

\item{ps_learner}{An mlr3 classification learner used as the propensity
score model, e.g. \code{mlr3::lrn("classif.log_reg")}. Its predict type is
set to \code{"prob"} automatically.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{folds}{Number of cross-fitting folds. Default \code{5}. Cross-fitting is
a core ingredient of DML; \code{folds = 1} (in-sample nuisance estimates) is
allowed but not recommended with flexible learners.}

\item{ps_trim}{Optional trimming for the estimated propensity scores.
Either a single value \code{a} (clips to \verb{[a, 1 - a]}) or a length-2 range.
\code{NULL} (default) applies no trimming.}
}
\value{
An object of class \code{"causalmlr_ate"}; see \code{\link[=ate_naive]{ate_naive()}}.
}
\description{
Estimates the average treatment effect in the partially linear model
\eqn{Y = \theta T + g(X) + \varepsilon} using the residual-on-residual
(orthogonalised) estimator of Chernozhukov et al. (2018). Two nuisance
functions are estimated with machine learning and cross-fitting:
the conditional outcome mean \eqn{m(x) = E[Y | X = x]} and the propensity
score \eqn{e(x) = P(T = 1 | X = x)}. The ATE is then
\deqn{\hat\theta = \frac{\sum_i (T_i - \hat e(X_i))(Y_i - \hat m(X_i))}
  {\sum_i (T_i - \hat e(X_i))^2}}
}
\details{
Note that unlike \code{\link[=ate_dr]{ate_dr()}}, the outcome model here regresses \code{Y} on the
covariates only (the treatment is excluded), and the model assumes a
constant (homogeneous) treatment effect.
}
\examples{
\donttest{
library(mlr3)
data(sodium)
set.seed(1)
ate_dml(sodium, outcome = "bp", treatment = "sodium",
        outcome_learner = lrn("regr.rpart"),
        ps_learner = lrn("classif.rpart"),
        ps_trim = 0.01)
}

}
\references{
Chernozhukov, V., Chetverikov, D., Demirer, M., Duflo, E., Hansen, C.,
Newey, W., & Robins, J. (2018). Double/debiased machine learning for
treatment and structural parameters. \emph{The Econometrics Journal}, 21(1),
C1-C68.
}
\seealso{
\code{\link[=ate_dr]{ate_dr()}}, \code{\link[=ate_ipw]{ate_ipw()}}
}
