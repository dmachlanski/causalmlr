% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metalearners.R
\name{s_learner}
\alias{s_learner}
\alias{predict.s_learner}
\title{S-Learner for CATE estimation}
\usage{
s_learner(data, outcome = "y", treatment = "t", learner, covariates = NULL)

\method{predict}{s_learner}(object, newdata, ...)
}
\arguments{
\item{data}{A \code{data.frame} with the outcome, treatment and covariates.}

\item{outcome}{Name of the outcome column. Default \code{"y"}.}

\item{treatment}{Name of the binary (0/1) treatment column. Default \code{"t"}.}

\item{learner}{An mlr3 regression learner for the outcome model, e.g.
\code{mlr3::lrn("regr.ranger")}.}

\item{covariates}{Optional character vector of covariate columns. Defaults
to all columns except the outcome and the treatment.}

\item{object}{A fitted \code{s_learner} model.}

\item{newdata}{A \code{data.frame} containing at least the covariate columns.}

\item{...}{Ignored.}
}
\value{
A fitted CATE model of class \code{c("s_learner", "cate_learner")}.
Use \code{\link[=predict]{predict()}} to obtain CATE estimates for new data and \code{\link[=ate]{ate()}} for
their average.
}
\description{
The S-Learner ("single learner") fits one outcome model
\eqn{\mu(x, t) = E[Y | X = x, T = t]} that includes the treatment
indicator as a regular feature. CATEs are obtained by contrasting the
predictions with the treatment switched on and off:
\deqn{\hat\tau(x) = \hat\mu(x, 1) - \hat\mu(x, 0)}
}
\section{Methods (by generic)}{
\itemize{
\item \code{predict(s_learner)}: Predict CATEs for new data. Returns a numeric
vector \eqn{\hat\tau(x)} with one element per row of \code{newdata}
(only the covariate columns are used).

}}
\examples{
\donttest{
library(mlr3)
data(synth_train)
data(synth_test)
m <- s_learner(synth_train, outcome = "y", treatment = "t",
               learner = lrn("regr.rpart"))
tau_hat <- predict(m, synth_test)
pehe(synth_test$tau, tau_hat)
}

}
\references{
Künzel, S. R., Sekhon, J. S., Bickel, P. J., & Yu, B. (2019).
Metalearners for estimating heterogeneous treatment effects using machine
learning. \emph{Proceedings of the National Academy of Sciences}, 116(10),
4156-4165.
}
\seealso{
\code{\link[=t_learner]{t_learner()}}, \code{\link[=x_learner]{x_learner()}}, \code{\link[=pehe]{pehe()}}, \code{\link[=r_loss]{r_loss()}}
}
