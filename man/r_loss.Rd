% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{r_loss}
\alias{r_loss}
\title{R-Loss: an observable score for CATE models}
\usage{
r_loss(nuisance, tau_pred)
}
\arguments{
\item{nuisance}{An \code{"rloss_nuisance"} object created with
\code{\link[=rloss_nuisance]{rloss_nuisance()}}, holding the outcome and treatment residuals.}

\item{tau_pred}{Numeric vector of CATE predictions for the same rows that
\code{nuisance} was computed on.}
}
\value{
A single non-negative number (lower is better).
}
\description{
Computes the R-Loss (Nie & Wager, 2021), also known as
\eqn{\tau\text{-risk}_R}, of a vector of CATE predictions:
\deqn{R\text{-}Loss = \frac{1}{n} \sum_i \left[ (Y^{(i)} -
  \hat m(X^{(i)})) - (T^{(i)} - \hat e(X^{(i)})) \,
  \hat\tau(X^{(i)}) \right]^2}
Unlike \code{\link[=pehe]{pehe()}}, the R-Loss does not require the true treatment effects,
so it can be computed on real observational data. This makes it suitable
for hyperparameter tuning and model selection of causal estimators:
lower values indicate better CATE models.
}
\examples{
\donttest{
library(mlr3)
data(synth_train)
set.seed(1)
nuis <- rloss_nuisance(synth_train, outcome = "y", treatment = "t",
                       outcome_learner = lrn("regr.rpart"),
                       ps_learner = lrn("classif.rpart"))

# compare two candidate CATE models by their R-Loss
m_s <- s_learner(synth_train, outcome = "y", treatment = "t",
                 learner = lrn("regr.rpart"))
m_t <- t_learner(synth_train, outcome = "y", treatment = "t",
                 learner = lrn("regr.rpart"))
r_loss(nuis, predict(m_s, synth_train))
r_loss(nuis, predict(m_t, synth_train))
}

}
\references{
Nie, X., & Wager, S. (2021). Quasi-oracle estimation of heterogeneous
treatment effects. \emph{Biometrika}, 108(2), 299-319.

Machlanski, D., Samothrakis, S., & Clarke, P. (2023). Hyperparameter
tuning and model evaluation in causal effect estimation. \emph{arXiv preprint
arXiv:2303.01412}.
}
\seealso{
\code{\link[=rloss_nuisance]{rloss_nuisance()}}, \code{\link[=pehe]{pehe()}}, \code{\link[=eps_ate]{eps_ate()}}
}
